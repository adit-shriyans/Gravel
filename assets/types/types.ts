export type MarkerLocation = {
    markerId: string,
    location: L.LatLngTuple,
    locationName: string,
    startDate?: string,
    endDate?: string,
    notes?: string
}

export type VoidFunctionType = () => void;

